import React from 'react';

function Button({ primary }) {
  // const ButtonPrimary = styled.button`
  //   /* Adapt the colors based on primary prop */
  //   background: ${(props) => (props.primary ? '#00aa5b' : 'white')};
  //   color: ${(props) => (props.primary ? 'white' : '#00aa5b')};
  //   font-size: 1em;
  //   margin: 1em;
  //   padding: 0.25em 1em;
  //   border: 2px solid #bf4f74;
  //   border-radius: 3px;
  // `;
  // return <ButtonPrimary primary={primary}>Masuk</ButtonPrimary>;
}

export default Button;
